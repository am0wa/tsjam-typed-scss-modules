const config = {
  // Note: Examples are nested. You would probably just need `__generated__` in the root of your package.
  outputFolder: "__generated__",
  // Example of sass-embedded async compilation.
  implementation: "sass-embedded",
  async: true,
};

export default config;
